sudo apt-get update
# For Sql-client
sudo apt-get install mysql-client

# For python and related frameworks

sudo apt-get install python3
sudo apt-get install python3-flask
sudo apt-get install python3-pip
sudo apt-get install python3-pymysql
sudo apt-get install python3-boto3

# for running application
sudo python3 EmpApp.py
For DSN name we can use www.freenom.com and register website free of cost for three months
next we need to list of nameserver which need to be updated in freenom these are getting from Route53 hostedzone
Post fill the nameserver detailsin freenom, exit from it and go to Route53 and create record 
